{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "sh -c 'if [ \"$SERVICE_MODE\" = \"bot\" ]; then python launcher.py; else uvicorn app.api.main:app --host 0.0.0.0 --port ${PORT:-8080}; fi'",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
